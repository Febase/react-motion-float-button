export enum Size {
  REGULAR = 56,
  SMALL = 40,
}

export enum Motion {
  SHOW_UP = 'show up',
  STRAIGHT = 'straight',
  CURVE = 'curve',
}

export enum Direction {
  TOP = 'column-reverse',
  BOTTOM = 'column',
  LEFT = 'row-reverse',
  RIGHT = 'row'
}